<%= form_for(:product_snooze, url: product_snoozes_path, remote: true, authenticity_token: true, html: { class: "pull-right", id: "remove-snooze-form" }) do   |f| %>
  <%= f.hidden_field :product_id, :value => @product.id %>
  <%= f.submit "Snooze", class: "btn" %>
<% end %>
