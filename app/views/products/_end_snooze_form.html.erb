<%= form_for(@product.product_snooze, remote: true, authenticity_token: true, html: { class: "pull-right", id: "remove-snooze-form" }) do   |f| %>
  <%= f.hidden_field :product_id, :value => @product.id %>
  <%= f.submit "Remove Snooze", class: "btn" %>
<% end %>
